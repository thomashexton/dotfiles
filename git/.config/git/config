[user]
    name = Thomas Hexton
    email = 25544371+thomashexton@users.noreply.github.com

[core]
    editor = zed --wait

[push]
    default = current
    autoSetupRemote = true

[pull]
    rebase = false

[filter "lfs"]
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f
    process = git-lfs filter-process
    required = true

[credential]
    helper = /usr/local/share/gcm-core/git-credential-manager

[pager]
    diff = diff-so-fancy | less --tabs=4 -RF
    show = diff-so-fancy | less --tabs=4 -RF
    branch = false

[interactive]
    diffFilter = "diff-so-fancy"

[color]
    ui = auto

[alias]
    parent = "!git show-branch | grep '*' | grep -v \"$(git rev-parse --abbrev-ref HEAD)\" | head -n1 | sed 's/.*\\[\\(.*\\)\\].*/\\1/' | sed 's/[\\^~].*//' #"

[maintenance]
    repo = /Users/thomashexton/work/employer
    repo = /Users/thomashexton/work/process-tooling
    repo = /Users/thomashexton/work/sarcophagus
    repo = /Users/thomashexton/work/docs
    repo = /Users/thomashexton/work/mobx
    repo = /Users/thomashexton/work/mobile-tools
    repo = /Users/thomashexton/work/thomashexton-dotfiles

[trace2]
	eventTarget = af_unix:dgram:/tmp/work-git-trace2.sock

[protocol]
	version = 2

[init]
	defaultBranch = main
